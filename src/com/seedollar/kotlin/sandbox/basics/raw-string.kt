package com.seedollar.kotlin.sandbox.basics

fun main(args: Array<String>) {
    val normalString = "This is a normal string"
    val rawString = """This is a "raw" string which can contain double quotes without being escaped"""

    println(normalString)
    println(rawString)

}